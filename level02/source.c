#include <stdio.h>

int main()
{
	stack frame = 288 (0x120)

	= {0};
	

	fd = fopen("/home/users/level03/.pass", 'r');
	if (fd == 0)
	{
		fwrite("ERROR: failed to open password file\n", 2, 24, stderr);
		exit(1);
	}
}
